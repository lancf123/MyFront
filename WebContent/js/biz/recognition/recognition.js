var test = {

	// 定义属性

	initBind : function() {

		// 绑定事件
	},

	// 更新事件
	updateEmployee : function(enterpriseId, uid, name, mobileNumber,
			authorityClosingTime, role) {

	},

	// 删除事件
	updateEmployee : deleteEmployee = function(enterpriseId, uid, roleKbn,
			name, mobileNumber, authorityClosingTime) {

	}
};

$(document).ready(function() {
	test.initBind();
});
